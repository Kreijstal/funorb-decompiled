package jagdx;

public class IDirect3DCubeTexture extends IDirect3DBaseTexture {
    public int LockRect(int face, int level, int x, int y, int w, int h, int flags, PixelBuffer buffer) { return 0; }
    public int UnlockRect(int face, int level) { return 0; }
}
